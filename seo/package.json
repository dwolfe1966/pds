{
  "name": "idlookup-seo",
  "version": "0.1.0",
  "private": true,
  "description": "IDLookup programmatic-SEO surface. Separate deployable; serves the /people tree (Census name×location directory) behind the path-split proxy.",
  "scripts": {
    "dev": "next dev -p 3005",
    "build": "next build",
    "start": "next start -p 3005",
    "verify": "npm run audit:claims && npm run build && npm run smoke:local",
    "verify:live": "npm run audit:claims && SEO_SMOKE_BASE=https://idlookup.me npm run smoke && SEO_AUDIT_BASE=https://idlookup.me SEO_AUDIT_TIMEOUT_MS=20000 SEO_AUDIT_CONCURRENCY=8 npm run audit:sitemaps && SEO_AUDIT_BASE=https://idlookup.me SEO_AUDIT_TIMEOUT_MS=20000 npm run audit:deep",
    "verify:live:expanded": "npm run verify:live && SEO_AUDIT_BASE=https://idlookup.me npm run audit:name-states && SEO_AUDIT_BASE=https://idlookup.me SEO_AUDIT_TIMEOUT_MS=45000 SEO_AUDIT_CONCURRENCY=2 npm run audit:counties",
    "smoke": "node scripts/smoke.mjs",
    "smoke:local": "bash scripts/smoke-local.sh",
    "audit:sitemaps": "node scripts/audit-sitemaps.mjs",
    "audit:deep": "node scripts/audit-deep-pages.mjs",
    "audit:counties": "node scripts/audit-county-pages.mjs",
    "audit:name-states": "node scripts/audit-name-state-pages.mjs",
    "audit:claims": "node scripts/audit-claims.mjs",
    "db:schema": "node scripts/apply-schema.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@sendgrid/mail": "^8.1.6",
    "next": "^15.5.21",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "undici": "^8.9.0"
  }
}
