---
name: project_homefacts_prototype
description: HomeFacts replacement built at idlookup.me/homefacts — area-profile pages on the SEO public-data engine; deck collateral for NIC CEO
metadata:
  type: project
---

**HomeFacts replacement — built + deployed to idlookup.me/homefacts, 2026-07-29.** Owner wants to take over HomeFacts.com (NIC-owned, [[project_homefacts_traffic]] / docs/partnerships/homefacts-*) and pitch the NIC CEO. Decision: don't just mock it — build a working replacement on our own SEO Next.js engine ("we can TOTALLY DO BETTER right now").

**What HomeFacts is:** search a city/ZIP/address → an area profile (neighborhood summary, schools, crime, property, environmental hazards, natural disasters, demographics, neighborhood info, sex offenders). Current site = Bootstrap 3 / PHP 5.3 / #337ab7 blue, dated. Home H1 "Get Reliable Neighborhood Information Instantly"; #1 traffic = offenders/person-name; monetizes via a "Local Background Checks" affiliate handoff to TruthFinder/Intelius.

**Our build (seo/ app, Next.js 15 RSC + ISR 60d + Neon, inline styles via lib/ui.js):**
- `app/homefacts/page.js` — landing: client typeahead (`HomefactsSearch.js`) over the ~2,067-city index built from `data/state-slice.json`; 9-module overview (5 Live / 4 Soon badges); popular cities.
- `app/homefacts/[state]/[city]/page.js` — the area profile, all 9 modules + jump-nav. **LIVE on real data:** neighborhood summary + demographics + property (ACS 5-yr via `lib/facts.js` `getCityAcs`), neighborhood info (Wikidata chips + population trend PopChart + StateMap + NPS historic + LoC newspapers + notable people), and **registered sex offenders** (first-party Neon via `querySexOffenders` — Austin showed 16 real records locally). **PENDING modules** (schools, crime, environmental, natural-disaster) render the section + honestly name the real public source being wired (NCES / FBI Crime Data Explorer / EPA EJScreen-ECHO / FEMA National Risk Index) — NEVER fabricated numbers.
- `lib/homefacts.js` — `propertyStats`/`demographicStats`/`HF_MODULES`/`hfCityPath`/`hfStatePath`.
- URL grain = **city** (state-slice + city-acs are place-level; NO ZIP/county ACS yet — search box shows an honest "ZIP/address coming next" hint). State seg is bare 2-letter → middleware (`/people`+`/profiles` only) leaves `/homefacts` untouched.

**Deploy:** committed (5915055) + pushed main → Vercel auto-deploys idlookup.me (repo=pds, root=seo). Production `npm run build` clean. **Additive only** — no changes to existing routes/sitemap/robots/middleware. **Deliberately NOT in the sitemap** (prototype: live + demoable without mass-submitting ~2k pages to the young, indexing-sensitive domain — see [[project_seo_indexing_incident]]; sitemap/index decision pending owner).

**GRAIN MODEL (owner 2026-07-29):** an "area-profile" (= "property-profile", same thing) can be a **city, county, ZIP, or address**. Owner wants **both city AND ZIP grain** (resolved the advisor's "surface as owner decision" fork — owner said do it). Better page IA = LATER (owner deferred).
- Planned routes: city `/homefacts/[state]/[city]` (LIVE); ZIP `/homefacts/zip/[zip]`; county `/homefacts/[state]/county/[county]`; address = a SEARCH input (geocode → ZIP/city profile), not a static page.
- **ZIP/address SEARCH shipped 2026-07-29** (keyless, commit c407658): `app/api/homefacts/resolve/route.js` — ZIP→Zippopotam, address→Census geocoder → covered-city (exact slug) or nearest covered city (haversine), honestly labeled; profile shows "Showing {City} — the area we cover for {ZIP/address}" via `?from=`. `HomefactsSearch.js` calls it for ZIP/address, city names stay local typeahead.
- ⚠️ **BLOCKER for real ZIP/county DATA:** ACS at ZCTA + county **requires CENSUS_API_KEY** (verified: keyless → "Missing Key"). Owner HAS one (fetch-acs uses it, passed on CLI) but it's NOT in seo/.env.local or (likely) Vercel env. **Need CENSUS_API_KEY in env** → then build zip/county pages via **fetch-at-generation** (ISR caches 60d per page → no 33k pre-ingest needed). Requested from owner.

**FEMA disasters — LIVE 2026-07-29 (6th module, commit 4482f1b).** `scripts/fetch-fema-nri.mjs`: NRI county table from ArcGIS FeatureServer `services.arcgis.com/XG15cJAlne2vxtgt/.../National_Risk_Index_Counties/FeatureServer/0` (query where=1=1, paginate resultOffset+2000, ~3,232 counties, 18 hazards `{HZ}_RISKR`; note riverine flood = **IFLD** not RFLD; overall = RISK_RATNG Very Low→Very High). City→county via **FCC block API** (lat/lng→STCOFIPS). Writes `data/city-fema.json` keyed "ST/slug" (2,040/2,070 cities, ~885KB, committed). **Batch/incremental save** (BATCH 250) — first attempt lost everything to a single end-of-run write that never fired. `lib/homefacts.js` `getCityFema`/`femaRatingColor`; profile renders overall pill + top-6 hazard severity bars. Modules now **6 live / 3 soon**.

⚠️ **ISR GOTCHA (cost a prod 500, fixed ce20020):** reading `searchParams` in an ISR page (`revalidate` + `generateStaticParams []`) throws **DYNAMIC_SERVER_USAGE** → 500 in PROD; **dev masks it** (renders fine). The `?from=` ZIP/address banner had to move to a CLIENT component (`FromBanner.js`, reads `window.location`) so the page stays static. RULE: never read searchParams/cookies/headers in these ISR page server components — use a client child. Also: **kill zombie `next` on :3005** (`lsof -ti tcp:3005 | xargs kill -9`) before testing — a stale server serving the old build gave false 500s during debugging. Verify prod-mode with `npm run build && npm start`, not just `npm run dev`.

**Schools — LIVE 2026-07-29 (7th module, commit 8b0d839).** `scripts/fetch-schools.mjs`: NCES Common Core of Data via **Urban Institute Education Data API** (educationdata.urban.org, KEYLESS but ⚠️ **403s node's default fetch UA → must send a browser User-Agent**). CCD directory only honors `state_location` (city/county filters IGNORED) → sweep per-state, bucket by normalized city_location into known city slugs → `data/city-schools.json` (1,924 cities, ~big; count + byLevel {Elementary/Middle/High/Other} + top-14 sample w/ grade span + charter/magnet). **No ratings** (NCES has none; ratings = paid GreatSchools, won't fake). enrollment often null in directory — omitted. Incremental per-state saves. Modules now **7 live / 2 soon** (crime, environment remain).

**Environment — LIVE 2026-07-29 (8th module, commit 6af5daf).** `scripts/fetch-epa-tri.mjs`: EPA **Toxics Release Inventory** via **Envirofacts efservice** (`data.epa.gov/efservice/TRI_FACILITY/STATE_ABBR/=/XX/rows/a:b/JSON`, KEYLESS, browser UA; fields LOWERCASE; filter format is `/COL/=/VAL/`, JSON suffix LAST). TRI = the meaningful hazard signal (Austin 89) vs all-registered FRS (8,131). Per-state sweep bucketed into city slugs → `data/city-epa.json` (count + sample names w/ county). ECHO REST was a dead end (ignores city param → counts all 344k; two-step QID API). ⚠️ **coverage still filling** — committed at 283 metros, sweep resumed to top up; **top up `data/city-epa.json` with a final commit when done**. Modules now **8 live / 1 soon**.

**Last module — Crime (FBI CDE):** needs a free **api.data.gov key** + agency/ORI→city mapping (hardest of the 9). Deferred.
**4-GRAIN MODEL — city + county + ZIP LIVE 2026-07-29 (commits 11e56e9 county, 6be303c zip).** Owner delivered the CENSUS_API_KEY (in seo/docs/credentials.rtf — gitignored; installed to seo/.env.local, also gitignored; NEVER commit either).
- **County** `/homefacts/[state]/county/[county]`: all-US via `data/counties.json` (3,232 counties from FEMA NRI; `scripts/fetch-nri-counties.mjs`). Live w/o key: FEMA disasters + sex offenders (Neon by county). ACS county via `getCountyAcs(fips)` fetch-at-gen.
- **ZIP** `/homefacts/zip/[zip]`: REAL ZCTA demographics+property via `getZctaAcs(zip)` (fetch-at-gen) + county FEMA (zip→latlng Zippopotam→county FCC→counties.json) + sex offenders by zip (added optional `zip` filter to `querySexOffenders`). Verified 78701 = $653,600 median home.
- **Address** = search input only (resolver → city profile); bare ZIP in resolver → `/homefacts/zip/{zip}`.
- City→county and zip→city/county cross-links wired.
- `lib/homefacts.js`: getCounties/countyFromSlug/countyForName/getCountyByFips, shared `fetchAcs`+`shapeAcsRow`, getZctaAcs/getCountyAcs (ACS_VARS = pop/age/HHincome/percap/homeval/rent/ownership).
- ⚠️ **PROD ACTION NEEDED:** ACS is fetch-at-generation reading `process.env.CENSUS_API_KEY` — **add CENSUS_API_KEY to the Vercel env** or ZIP/county ACS sections stay empty in prod (pages still render disasters+registry). Local works via .env.local.

**IA REDESIGN — DONE 2026-07-29 (commit eb96294).** New design system `lib/hf.js`: sticky **Homefacts** header+wordmark, report-card `SummaryBand` (headline metrics), sticky `SectionNav`, clean `Section` modules (eyebrow+title+sourced footnote, NO emoji markers), FEMA risk ramp = the one semantic color. **Brand is ONE flip-able token block** (`BRAND`/`THEME` in hf.js) — defaults to evolved-**Homefacts blue #12507e** to match the CEO deck; `THEME='idlookup'` → site green. Applied to landing + city + county + zip + search box. ⚠️ open one-liner for owner: keep Homefacts-blue or reskin to idlookup-green?

**Work queue (owner order 3→1→4→2→+5) — ALL DONE 2026-07-30.** 3 IA ✅ · 1 Crime ✅ (FBI UCR/NIBRS via CDE summarized/agency; city→ORI map `data/city-ori.json` from fetch-city-ori.mjs, 1,816 cities; `getCityCrime` fetch-at-gen agency-vs-state-vs-US) · 4 state hubs `/homefacts/[state]` + `/sitemap-homefacts.xml` (2,120 quality URLs) + robots ✅ · 2 county ACS pre-cached `data/county-acs.json` (3,144, no runtime key) ✅ · 5 popular names folded into city page ("People searches in {city}" → /people) ✅.
**PLUS (owner VIP 2026-07-30):** **Incarceration module** (city + county pages: rosterTopNamesByCounty → /people county profiles; 5s timeout; first-party moat) + **county SEARCH** in typeahead (was city-only; landing passes countyIndex). Crime endpoint saga: base is `api.usa.gov/crime/fbi/sapi/summarized/agency/{ORI}/{violent-crime|property-crime}?from=MM-YYYY&to=MM-YYYY&api_key=` (NOT /cde/; found via fbi-cde/crime-data-frontend src/util/api.js). FBI_CDE_KEY installed to .env.local.
**Meeting brief Artifact** (9ef960fa-52db-405a-97ca-930772909f07): HomeFacts + PS/BC funnels + week-in-review.

**Prod:** ⚠️ still need **CENSUS_API_KEY in Vercel env** for ZIP/county ACS to render in prod (works locally). EPA coverage committed at 283 metros (top-up sweep pending re-run).

**Data-sweep gotchas (reusable):** (1) many gov APIs 403 node's default fetch UA — send a browser UA. (2) write the cache INCREMENTALLY (per-batch/per-state) — a single end-of-run write loses everything if the process is reaped. (3) background `nohup` sweeps can fire a premature "completed" notification while STILL running (`pgrep -f <script>` to check) — the DONE log line is the real signal.

**CEO collateral:** redesign-concept Artifact (before→after, offender/records money-page, place×person bridge) — brand stays HomeFacts (evolve their blue, not IDL green), no "competitor" framing (thin-economics/LTV, Zillow is the real competition), only sourced numbers. Deck docs in docs/partnerships/homefacts-*.

**2026-07-30 meeting-day final:** More-people pass done — state hub incarceration+names (like /people/{st}), enriched ZIP (schools/crime/incarceration/names for its city+county), incarceration on city/county/zip, county SEARCH in typeahead. **Maps:** `lib/AreaMap.js` (OSM iframe embed, zero-dep) on city+zip; Leaflet offender-pin map = fast-follow (offenders carry lat/lng). **Crime PRE-CACHED** `data/city-crime.json` (392 cities, fetch-city-crime.mjs) — getCityCrime reads cache first, so crime renders in prod WITHOUT the Vercel FBI key (same pattern as county-acs). ⚠️ TX/AZ/etc counties have NO incarceration roster (only FL/NC/IL/PA roster states) → incarceration section correctly hidden there. Vercel: CENSUS_API_KEY confirmed working (ZIP ACS live); FBI key now moot for cached cities. A new deploy purges ISR cache → all pages regenerate fresh.

**2026-07-30 CEO deck + offender map:** (1) **CEO partnership deck** Artifact 61c4b501-e934-4937-a8aa-73366fe5c936 — merit-based/minimal-assumptions (NIC CEO hired by Tim/Lauren Chin who founded NIC + invests in IDL — that's CONTEXT, kept OUT of deck; pitch stands on economics). Crisp on: share **LTV of IDL subs** (not per-lead), **"Where"(NIC/place) + "Who"(IDL/person)** merged best-in-class, the **people-intent searches** HomeFacts captures (person-name/SO/neighborhood-safety/background), and **idlookup.me/homefacts = MVP/proof-of-concept**. David Wolfe framing: **former CPO at MyLife, built SEO strategy → 20M/mo Google visitors 2014-2018** (that exact phrasing, nothing more). (2) **Sex-offender Leaflet map** (`app/homefacts/OffenderMap.js`, added `leaflet` dep): pin per offender (records carry lat/lng, ~10/16 have coords), OSM tiles, CSS divIcon (no marker-png 404), dynamic import (no SSR), CDN leaflet.css. On city offenders section above the list. AreaMap (OSM iframe) stays for general location. ⚠️ Playwright times out on the dual-map heavy page — verify visually in a real browser at idlookup.me/homefacts/fl/miami.

**2026-07-30 meeting deliverables (final):** Offender Leaflet map extended to ZIP + county pages (was city-only, commit 01965f1). CEO deck v2 (61c4b501) added: old-vs-new before/after comp slide + MVP-clarity framing (a: replicate HF fast, b: preserve hierarchy/taxonomy, c: same data sources; design polish post-pilot). **Meeting links hub Artifact 9119c6fc-ec77-4c06-8a06-69c3c0a13274** — 3 pressos (CEO deck 61c4b501 · week-in-review brief 9ef960fa · PS/BC flows f7b23036) + live HF sample links (city/miami-offender-map/zip/broward-county/tx-state) + 3 PS flow experiences (idlookup.ai/people-search·proof-check·my-exposure, all 200). ALL urls verified live.

**2026-07-30 more HF additions:** (1) **FAQ + FAQPage JSON-LD** on city profiles (`cityFaqs` in homefacts.js) — real data-grounded Q&A (population/home value/crime-vs-national/disaster/schools/offenders), visible section + schema for Google rich snippets / People-Also-Ask. (2) **"Cities in {county}"** hub links on county pages (`citiesInCounty` matches cities→county via city-fema.json county name) — internal linking + taxonomy hub. (3) **landing-page-flows.md UPDATED** (docs/design/) — added PS challengers (honest/proof/self + ?shn) + HomeFacts MVP section + example URLs; this is THE catalog of all landing experiences.

**2026-07-30 DESIGN POLISH (iconography + imagery, SEO/perf-safe = inline SVG only):** `lib/HfIcon.js` = monochrome line-icon set (one per module, currentColor). `lib/hf.js` `TopoMotif` = reusable topographic-contour SVG (color/opacity props). Applied: (P1) module-header icon chips + SectionNav pill icons + schools re-swept w/ lat/lng → schools MAP (blue pins, generalized OffenderMap w/ color prop). (P2) landing HERO — topo motif over gradient, icon eyebrow, coverage chips (City/County/ZIP/Address), data-source trust line (Census·FBI·FEMA·EPA·NCES), module-card icons. (P3) SummaryBand metrics carry module icons; city/county/zip HEROES = tinted accent-gradient header card + topo motif + icon eyebrow. NO raster imagery (per-city photos unavailable + perf). ⚠️ product-quality-reviewer design agent was running — apply its findings as further refinement. Relatives/associates = NOT a HomeFacts area-page module (no subject person); belongs on the /people person leaf pages the area pages link into.

**2026-07-30 CSR bug + HF round (owner asks):** (CSR) CSR reply email links `/contact?type=contactCsrReply&hash=&contactMessageId=` landed on Contact Us (params ignored). FIX (commit b8dd254, src/App.js): `ContactRoute` wrapper detects deep-link (24-hex id + hash) → `<Navigate replace>` to `/contact/thread/:id` (hash-authed ContactThreadPage, works logged-out) preserving query; ordinary /contact links untouched. Ships on BC bundle upload; live hash-fetch unverified (Turnstile). Judgment call flagged: routed to /contact/thread not /account (email recipients usually logged out). (HF) #1 homepage stat band (real aggregates: 3,232 counties, 42K+ schools, 9K+ TRI sites) + Where+Who value section + browse-by-state (52). #2 ZIP pages pull city Environment (EPA) too (now full city module set). #3 ADDRESS experience: resolver passes `?alat=&alng=`; `AreaMap` (now 'use client') re-centers on address w/ marker + "Your address" caption (SSR area-centered → effect tightens, hydration-safe); `OffenderMap` drops a home pin + "X.X mi from your address" in every offender/school pin popup + centers on address. Address reuses the city page (not its own route). ⚠️ address maps are client-JS — verify visually (browser tool flaky on heavy pages).

**2026-07-30 address neighborhood-data hunt + build:** Owner: hunt address-based neighborhood data (active crime, entertainment). VERIFIED keyless-by-lat/lng: OSM Overpass (entertainment/amenities, nationwide — main instance rate-limits, use maps.mail.ru mirror), city Socrata crime (incident-level, big metros only), NWS weather, Wikipedia geosearch, FCC. Retired/keyed: OpenAQ v2 dead (v3 keyed), Walk Score/Yelp/Foursquare keyed. BUILT (all client-only on address searches, ISR-safe, render nothing without ?alat/alng or on failure): `AddressSafetySummary` (offenders within 1/3 mi + nearest school), `NearestToAddress` (distance-sorted offender/school lists), `WhatsNearby` (Overpass dining/nightlife/entertainment/parks by radius, mirror fallback chain), `RecentCrime` + `crimeSources.js` (Socrata incident crime by ~0.5mi bbox — registry: chicago ijzp-q8t2, los-angeles 2nrs-mtv8 [data stale 2024], new-york 5uac-w243, san-francisco wg3w-h783; extensible; austin has NO coords). Also `OffenderMap`/`AreaMap` now orient to address (home pin + distances). ⚠️ browser tool CANNOT load idlookup.me map pages (60s timeout all session) — all address features build-verified/SSR-stable but need owner's-browser eyeball.

**2026-07-30 weather/AQI/landmarks + crime expansion:** BUILT client modules (all keyless, additive, render-nothing-on-fail): `Weather` (Open-Meteo forecast — NOT NWS: browsers can't set required UA; Open-Meteo is CORS-friendly), `AirQuality` (Open-Meteo air-quality-api, US AQI + EPA band + PM2.5/ozone), `Landmarks` (Wikipedia geosearch, origin=* for CORS, links to WP). Weather/AQI/Landmarks render on ALL city+zip pages (area center, address-aware) — NOT just address. Crime registry `crimeSources.js` now 6 metros (added Cincinnati k59e-2pvf latitude_x/longitude_x, Seattle tazs-3rd5). Denver moved to ArcGIS (skip); Austin/Dallas public datasets lack coords. **Full neighborhood-data layer now:** 9 core SSR modules + Weather + AQI + Landmarks (all pages) + RecentCrime + WhatsNearby + AddressSafetySummary + NearestToAddress (address searches) + address-oriented maps. ALL free/keyless — no API cost.

**2026-07-30 more data (earthquakes/alerts/nearby+):** `Earthquakes` (USGS fdsnws, keyless, ~50mi/3yr/mag≥2.5, SELF-GATES to seismic areas — nothing where none), `WeatherAlerts` (NWS alerts/active?point, keyless, SELF-GATES to active alerts, graceful if NWS UA-blocks). Enriched `WhatsNearby` (Overpass) with Health (pharmacy/hospital), Essentials (supermarket/bank/fuel/library), Getting-around (transit/rail/bus). **Keyless-data hunt now largely exhausted** — remaining candidates are fiddly/keyed: FEMA flood-point (behind Tivoli auth gateway on hazards.fema.gov — needs the public ESRI NFHL endpoint), FCC broadband (National Broadband Map now keyed), radon (EPA county zones — static ingest), more crime metros (schema variance, diminishing). Data layer is very complete for MVP.

**2026-07-30 FEMA flood-point (final data add):** `FloodRisk` — FEMA NFHL flood zone by point. ⚠️ CORRECT endpoint = `hazards.fema.gov/**arcgis**/rest/services/public/NFHL/MapServer/28/query` (the `/gis/` path is behind a Tivoli auth gateway → 404). outFields FLD_ZONE,ZONE_SUBTY,SFHA_TF. Interpret: SFHA_TF='T' or zone A*/V* = High (100-yr SFHA, insurance req); ZONE_SUBTY has "0.2 PCT" = Moderate (500-yr); zone X = Minimal; D/none = undetermined/unmapped (self-gates). Verified Miami AE-high / New Orleans 500-yr / Denver unmapped. Client, area-center + address-aware, in disasters area. **DATA LAYER DONE per owner** — keyless well exhausted.

**2026-07-30 crime coverage fix:** Owner "not seeing crime data" = the FBI crime-RATES module (#5) was cached for only 392 cities (top-400 sweep) → ~1,400 cities showed the "coming soon" pending fallback. FIX: ran fetch-city-crime.mjs to completion → **1,755 cities** cached (all with a reporting police ORI; non-reporting agencies like Berkeley stay honestly pending). Also confirmed **Socrata sends ACAO:* ** so the address-level incident-crime (RecentCrime, 6 metros) works in-browser. Two distinct crime features: (1) crime RATES module = every city, now 1,755 covered; (2) incident/active crime = address-search only, 6 metros.
