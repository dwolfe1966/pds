---
name: project_seo_recovery
description: SEO is floundering — recovery brainstorm; pivot from generic people-directory pages to first-party data verticals + authority
metadata: 
  node_type: memory
  type: project
  originSessionId: c559d3ef-73d6-4bd2-ae57-b22ea4c6528f
---

Owner 2026-07-20: SEO "floundering," wants to recover + grow organic traffic. Brainstorm in `docs/seo/seo-recovery-brainstorm.md`.

**Diagnosis:** the SEO app (idlookup.me, Next.js+Neon) is 100% GENERIC people-directory pages (`/people/[state]/[city]/[name]/[id]` + a duplicate `/profiles/...` tree) — the most saturated, thinnest, least-differentiated content in the space. New domain = ~0 authority; can't out-rank Spokeo/TruePeopleSearch on their own page type. 360k thin pages → the 7/13 helpful-content impressions collapse ([[project_seo_indexing_incident]]). Meanwhile the first-party data moat (`lib/`: incarceration, sexOffender, life-events, person-search) powers NO SEO page.

**Thesis:** recovery on a new domain = FEWER, DIFFERENTIATED pages + AUTHORITY, not more pages.

**Recommended hybrid (works regardless of pivot-vs-fix appetite — owner didn't answer that yet):**
- **A. Stop the bleed:** prune/noindex the thin long-tail; collapse the duplicate /people vs /profiles trees (301 one); evaluate domain consolidation (idlookup.me vs idlookup.ai/people path-split).
- **B. Differentiate survivors with first-party data:** augment pages with our records; **lead vertical = INMATE** (facility rosters, "inmates named X in [state]", DOC hubs) — unique data + proven 7.15%-CVR intent + pipelines already exist (`stateInmates.mjs`, `inmatesDb`), just not rendering pages. NOT Turnstile-gated (unlike BC teaser) → renderable server-side.
- **C. Build authority (the real bottleneck):** editorial how-to content ("how to find an inmate in [state]", "remove your info from data brokers") — earns links, lower competition, funnels to tools.
- **STOP:** expanding generic name×state pages.

**Owner selected goal:** "Recover + grow organic traffic." **Refined direction (owner 2026-07-20):** DON'T blanket-noindex the valuable pages. `/profiles` REMOVED (was the abandoned IDI test). Tier 5 individual pages DON'T EXIST (route always 308→Tier-4; needed IDI data). Tier 3 (city) + **Tier 4 (name-in-city) are the MOST valuable** (Tier 4 = the "[Name] in [City]" query match). So **dedup + differentiate first, noindex only the residue.**

**Owner agreed dedup sequence 1-2-3-4:** (1) differentiate w/ first-party data, (2) consolidate/raise thin gate, (3) canonical hygiene, (4) de-template. Preceded by a MEASURE step.

**MEASUREMENT (2026-07-20):** Tier-4 page = ~85% TWO reusable boilerplate blocks — per-NAME (first-name/surname facts cards + prose, identical across all cities for a name) + per-CITY (ACS "About city" + "Other names in city", identical across all names in a city) — with only ~10-15% combo-unique (H1 + estInCity number + usually-empty captured-profiles). → near-dup on BOTH axes. Gate is low (EST_IN_CITY_MIN=2). That boilerplate:unique ratio is the helpful-content liability.

**STEP 1 DONE (commit 0c4d20e):** folded first-party incarceration records ("[Name] in [State]", `inmatesDb.queryInmates`) onto Tier-4, ABOVE the boilerplate. Combo-unique, no competitor has it, self-gating. **Coverage reality:** broad named roster ~only FL (OBIS bulk) + partial NC; most states sparse (on-demand) → lights up FL now, grows as more state rosters bulk-ingested (moat work; see [[project_incarceration_data_moat]]). Note: state-keyed → breaks name-axis near-dup within a city; city-axis still shares (refine w/ county-level later).

**STEP 1 PIVOTED TO NAME-IN-STATE (commits c9b5ff7, a8e6798):** eyeball found (a) DATA-SOURCE bug — FL OBIS is in `fl_inmates` (floridaObis provider), NOT the `inmates` table queryInmates reads → added `rosterByNameState()` (DB-only: fl_inmates+inmates, ISR-safe) in incarceration.mjs; (b) GRAIN — incarceration is state-grain → belongs on name-in-STATE (`/[state]/[name]` + `/people/[state]/[name]` fallback → NameInStateView, now async), NOT name-in-city (removed from there). LIVE: idlookup.me/people/fl/michael-smith shows 12 real FL incarceration records (ages/charges/counties). County surfaced (extracted from OBIS charge text). Coverage: FL strong, grows w/ roster ingestion.

**VIP — ZERO/THIN-MATCH RESCUE (owner 2026-07-20, commit 8b82679, consumer bundle public.7a04de4b.js NOT yet on BC):** the SignalTeaser was only in the has-results branch of the sales SERP → common names hitting IDI TooManyMatches → dead-end. Now renders the engine teaser (FIRST-PARTY inmate/divorce, IDI-independent) on the ZERO-STATE branch too → fills the dead-end with real records + conversion path exactly when BC returns nothing. **Completes the funnel: SEO name-in-state / inmate-ads (7.15% CVR) → idlookup.ai → TooManyMatches → first-party teaser → convert.** Also RESCUES the name-in-state "nebulous CTA" (it lands here). This is the resilient-zero-result backlog item, partially realized via the existing SignalTeaser engine.

**Owner page-thickness assessment (2026-07-20):** `/people` + `/people/[state]` = THIN (enrich). `/people/[state]/[city]` + `/[state]/[name]` = thicker (make thicker still — more data). `/people/[state]/[city]/[name]` = TBD. Individual profiles = longer-term goal. More data for name-in-state candidates: mugshots (deferred — broken-image risk in SSR w/o client onError; needs validity check), aggregate incarceration stat, sex-offender (high-stakes).

**ENRICHMENT BATCH (2026-07-20, all LIVE on idlookup.me):** (1) name-in-state pages: mugshots (bg-image, graceful gray fallback — no broken-img SSR icon) + aggregate stat in intro ("N have public incarceration records in {state}") [77694e3]; (2) `/people/[state]` hub: "Incarceration & inmate records in {state}" section, getStateTopNames(30) → name-in-state links [668f21a]; (3) `/people` root thickened: richer intro + "What you can search" record-types + "Browse by state" [f2cd14b]. EYEBALLED live: idlookup.me/people/fl/michael-smith = 12 real records, 9 mugshots render + 3 gray-fallback, counties/ages/charges all correct.
**SITEMAP FIX (2026-07-20, critical):** `getDirectoryUrls` emitted /people+state+city+name-in-CITY but NOT name-in-STATE → our ONE differentiated page type was undiscoverable by Googlebot. Now emits ~5.2k name-in-state URLs (getStateTopNames 100/state); trimmed name-in-city cap 45k→42k to stay <50k/sitemap (name-in-state wins the trade). VERIFIED live: sitemap-directory.xml = 48,662 URLs, michael-smith/fl + james-smith/tx present.

**STEP 2 — NAME-IN-STATE HALF DONE (2026-07-20, LIVE):** discriminator = a name-in-state page is indexable IFF ≥1 first-party incarceration record; else per-page `robots:{index:false,follow:true}` (follow keeps link equity + funnel). Mechanism: `rosterByNameState` wrapped in React `cache()` → generateMetadata (robots) + body share ONE DB hit; ISR caches 60d; pages auto-flip to index as rosters grow. Sitemap emits name-in-state only for `ROSTER_STATES={fl,nc,il,pa}` (page-level robots still indexes real records in any other state, e.g. CA david-johnson). VERIFIED local: fl/michael-smith=index, wy/john-smith=noindex, ca/david-johnson=index(1 CA rec).
**STEP 2 — ROSTER COVERAGE MEASURED (2026-07-20):** fl_inmates=670,606 (FL deep, 20/20 common names hit ~96 recs/name); inmates=452,716 rows across 34 states but concentrated NC=448,037 / IL=1,501 / PA=1,127 / rest sparse (<400). So indexable name-in-state ≈ FL+NC (+IL/PA common) ≈ few hundred; ~4,900 residue noindexed.
**STEP 2 — NAME-IN-CITY DONE (owner picked A, 2026-07-20, LIVE):** same mechanism — indexable IFF ≥1 captured individual (person_profiles) for that name+city; else `robots:{index:false,follow:true}`. getCapturedPeople wrapped in React `cache()` (shared metadata+body). Only ~980 of ~42k combos have captures (person_profiles=1,033 rows, mostly UNUSUAL names that don't even map to slice pages → 404). Sitemap: dropped name-in-city entirely (`maxNamePages:0`) — listing 41k noindex URLs is an anti-signal; the ~980 stay discoverable via each city page's top-60 name links. VERIFIED: oh/cleveland/john-smith(captured)=index, tx/houston/john-smith(no capture)=noindex, arlene-scott=404. STEP 2 COMPLETE (both tiers).

**CA STATE-HUB BUG FIXED (2026-07-20, LIVE):** owner "not seeing incarceration info on /california/names" → (1) `/california/names` 404s by design (routes key on 2-letter code, /people/ca). (2) REAL bug: state-hub incarceration names section used `getStateTopNames` which applies EST_IN_STATE_MAX=10000 (consumer-search TooManyMatches cap) → in big states every common name exceeds 10k in-state → excluded → CA section empty despite real records (CA james-smith=13, john-smith=8). FIX: new `rosterTopNamesByState()` in incarceration.mjs sources the section from the actual roster (fl_inmates+inmates), ranked by record count, self-gating, links only to pages w/ records (→indexable). Shows counts. Verified /people/ca lists james-smith(13) etc.; /people/wy empty.

**STEP 2 — NAME-IN-CITY (original decision note):** the BIG fish — ~42k name-in-city pages are now ~100% boilerplate (I moved inmate diff OFF this tier; person_profiles=only 1,033 rows covers ≤1k pages). Left FULLY INDEXED pending owner's call (asked via AskUserQuestion, no answer): (A) noindex ~41k residue now, index only ~1k w/ person_profiles [recommended — dominant quality drag, reversible]; (B) differentiate w/ county inmate data first (inmates.county col; FL/NC cities only) then noindex rest; (C) leave indexed [Step 2 stays cosmetic]. Owner earlier called Tier-4 "most valuable" → why it's their call. Sitemap still lists 42k name-in-city until decided (route.js comment flags it).

**MAXIMIZE-DATA PASS (2026-07-20):** owner "maximize data (everything we have)" on SEO pages. TASK 1 DONE (LIVE): deepened name-in-state incarceration records — new shared `lib/inmate-records.js` `<InmateRecordsSection>` surfaces every roster field (mugshot, facility+county+state, release/custody status, booking date, sex, race, ≤4 charges +N). Fields self-gate per source (FL OBIS=charges+county-in-text no booking; NC=status+sex+race no charges). Verified nc/james-smith renders Male/White/Black. FIELD COVERAGE (inmates 452k rows, ~NC): release_status 99%, race 99%, sex 99%, booking_date ~0% (235), county 0.3% (PA/CA). TASK 2 (county-grain name-in-city) NOT SHIPPED — advisor caught fatal flaw: incarceration data has NO city field (finest grain=county), so county records on city URLs duplicate across every city in a county = the near-dup Step 2 killed. Also the est cap (cityNameOk MAX=10000) 404s the common record-rich names anyway. County data belongs on name-in-state (done) or a NEW county-hub taxonomy `/people/{state}/county/{county}/{name}` (owner's call — pending). Reverted cleanly.
**SEX-OFFENDER on SEO (owner cleared DISPLAY rights 2026-07-20):** BLOCKED on data plumbing — `lib/sexOffender.mjs` (NSOPW) is a LIVE Cloudflare-gated API needing BROWSER_SERVICE_URL + residential proxy → NOT ISR-safe for SEO pages. No ingested SO table in Neon (only live/`life_events_cache` per-lookup). Needs an NSOPW→Neon ingestion pipeline (FL-OBIS pattern; location/state+county searchable → fits state/city pages). Pending owner go on building the pipeline.
**REAL LEVER = COVERAGE not URL geometry:** name-in-state already exposes everything we have; big states thin (TX=9,NY=8,GA=1 names). Ingesting more state rosters (moat) grows differentiation more than any new page shape.

**OWNER DECISIONS (2026-07-20): build all three, order 3→2→1.** Thin-match teaser DEPLOYED (public.7a04de4b.js live, "looks better").
**#3 COVERAGE SWEEP (in progress 2026-07-20):** `crawl-state-inmates.mjs` works from the local Mac IP (residential, gov sites don't block). Ran `run-coverage-sweeps.sh` (seo/ root, uncommitted scratch) — top-50-surname seed per state through live DOC adapters → upsert `inmates`. YIELDS: CA 197→2,500, PA 1,150→10,792; sweeping IL/WA/GA/OH + VINE (NY/NJ/KY/CT/NH/TN/WV/MN) + TX(first×last). Adapters confirmed working: CA/PA/IL/GA/OH/WA/NY/NJ. TX=NEEDS_FIRST (surname-only returns 0). Captcha states (OK/NM/KS/WI/CO/DE) skipped. Each surname caps ~50 (CDCR)/25 (VINE) → seed not exhaustive; full A–Z enumeration is follow-up.
**#2 SO PIPELINE — SCAFFOLDING BUILT + COMMITTED, BLOCKED ON INFRA:** db/sex-offenders-schema.sql (location-native `sex_offenders` table, CREATED on Neon), lib/sexOffenderDb.mjs (upsert + optional-filter query serving name/county/city grains + count + takedown), scripts/crawl-sex-offenders.mjs (top-surname NSOPW sweep). BLOCKER: `BROWSER_SERVICE_URL` = Browserless FREE plan at usage cap (401); NSOPW is Cloudflare-gated so needs PAID browser tier + residential proxy. OWNER ACTION: upgrade Browserless. SEO display wiring deferred until real NSOPW records eyeballed (shapes unconfirmed live). NSOPW record shape (toRecord in lib/sexOffender.mjs) has name+city+county+state+zip+photo+registryUrl; offenses empty (flyer-only).
**#1 COUNTY-HUB — NOT STARTED (next).** Plan: `/people/{state}/county/{county}/{name}` (+ county listing hub) showing county-scoped incarceration — one URL/county (no cross-city dup). Re-add `rosterByNameCounty` (was built+reverted; county from inmates.county PA/CA/GA + FL charge-text). Needs county resolution + sitemap + internal links. County coverage: FL(charge-text) + PA/CA/GA(county col); NC null county.

**UPDATE 2026-07-20 (later): all three progressed.**
**#1 COUNTY-HUB — BUILT + LIVE.** Incarceration's finest grain is COUNTY (records carry county not city → no cross-city dup). New routes: `/people/{state}/county/{county}` (hub: ranked names w/ records) + `/people/{state}/county/{county}/{name}` (name-in-county records). Static `county` seg resolves before `[city]` (verified /people/fl/miami intact). Backfilled `fl_inmates.county` from OBIS charge text (667,829 rows/81 counties; db/fl-county-backfill.sql). lib fns: rosterByNameCounty/countiesByState/rosterTopNamesByCounty/countyFromSlug/allCountyHubUrls/countySlug + countyTitle. queryInmates/floridaObis gained optional county filter. State hub has "records by county" section; sitemap emits 321 county hubs (FL/PA/CA/GA/IL). LIVE: /people/fl/county/miami-dade + .../jose-rodriguez (20 recs, index).
**#2 SO PIPELINE — UNBLOCKED (Browserless upgraded) + SEEDING.** Fixed 2 bugs live: (1) NSOPW needs first×last (surname-only=status103); (2) ON CONFLICT DO UPDATE can't touch same id twice → dedupe by id before batch (NSOPW returns offender under multiple locations). Nationwide seed running (20×30=600 combos, ~2.5hr); 6,981 rows across 65 jurisdictions and climbing. Records carry name/age/city/county/state/zip/photo/registry. STILL TODO: wire SO display onto state/city/name pages (defer'd for real-data eyeball — now have data), + finish/expand seed.
**#3 COVERAGE SWEEP — big yields.** CA 197→2,500, PA 1,150→10,792, IL →10,346, WA 1,967, NY 1,216, KY/CT/NJ/GA/OH added; NH/TN/WV/MN + TX(first×last) still running. Top-surname seed (not exhaustive).

**#2 SO DISPLAY — BUILT + LIVE (2026-07-20).** Shared `lib/sex-offender-section.js` `<SexOffenderSection>` (photo+graceful fallback, name/age, cleaned location [JUNK filter drops INCARCERATED/HOMELESS/UNKNOWN/OUT-OF-STATE/00000], ABSCONDER badge, AKA, per-record "Official registry record →" link, source + responsible-use disclaimer). Wired: name-in-state (SO by name, also flips robots→index), city page (SO by city — SO has real city so combo-unique, no dup), state page (aggregate count in intro). Self-gating. Eyeballed live /people/fl/miami (16 offenders, mugshots render, absconder badge, registry links) — looks clean/professional. Coverage grows with seed (~13k rows). querySexOffenders/countSexOffenders in sexOffenderDb.mjs.

**SITEMAP/INDEXING (2026-07-20):** sitemaps are DYNAMIC Next routes (auto-update on deploy), no manual recreation. robots points at sitemapv2.xml (~1k) + sitemap-directory.xml (quality core = 2,283 URLs after Step-2 dropped name-in-city: states+cities+roster-state name-in-state+321 county hubs). NOT the old /sitemap.xml index (360k thin — deindex flood, keep OUT). IndexNow was ALREADY set up (key file app/6c3e…txt route + scripts/indexnow-submit.mjs). Added `--scope=directory`; bumped sitemap-directory LASTMOD 07-18→07-20 (real content change). SUBMITTED directory scope → 2,283 URLs accepted HTTP 200 (Bing/Yandex/Seznam via api.indexnow.org). IndexNow ≠ Google (Google uses sitemap+GSC; lastmod bump nudges re-crawl). Owner may resubmit sitemaps in GSC/Bing Webmaster to nudge, optional.

**STEP 3 CANONICAL HYGIENE — DONE (2026-07-20).** Audited live: every tier self-canonicals; BOTH name-in-state forms (`/{state}/{name}` root + `/people/{state}/{name}`) → ONE `/people/` canonical (consolidation solid); path builders lowercase state code; host/protocol/trailing-slash all redirect (http→https, www→apex, /x/→/x); `/profiles` 404s; Tier-5 `[id]` 308-redirects invalid ids. Clean — no changes needed.
**PARKED (needs OWNER GSC fact, advisor 2026-07-20):** old `/sitemap.xml` index still serves 200 = 9 chunks × 45k = ~360k thin name-in-city URLs (now noindex). robots excludes it; pages noindex + internally linked. DON'T yank the thin URLs from getSitemapUrls yet — Google must RE-CRAWL a noindex page (sitemap+lastmod is the accelerant) to DROP it, so removing them could SLOW the Step-2 deindex. Decision hinges on GSC: are those ~360k "Indexed" (→ keep crawlable / temp removal-sitemap w/ fresh lastmod to flush) or "Discovered–not indexed" (→ nothing to flush, serving quality core is fine). OWNER: pull that GSC status.

**#2 COVERAGE EXPANSION — IN PROGRESS (2026-07-20, chained autonomous jobs).** Pipeline VERIFIED end-to-end: new coverage auto-lights-up name-in-state (IL→10k made /people/il/james-smith show "Illinois (8)" + index). 3 background jobs: (1) wave-1 inmates sweep finishing (was on WV → MN+TX left); (2) `run-coverage-wave2.sh` (seo/ root, chained: waits for wave-1, then surnames 51–100 for CA/PA/IL/WA/OH/GA/NY/NJ/KY/CT + fuller TX first×last); (3) SO seed (~19k rows, 153/600). NOTE: adapters only exist for ~16 states (TX/CA/PA/IL/NY/NJ/KY/CT/NH/TN/WV/MN/WA/OH/NC/GA) + 6 captcha (OK/NM/KS/WI/CO/DE); more states = build adapters (big moat work). IL/NY county-hub self-gates (no inmates.county). SO grid expansion beyond 600 combos = Browserless COST decision for owner. Coverage from LOCAL Mac IP (residential, gov sites don't block).
**HAMMER-THE-STATES (owner 2026-07-20, unlimited budget): ~51 adapters ALREADY EXIST (built 7/19), just weren't crawled.** GitHub Action `crawl-inmates.yml` verified: was scoped to 7 browser/captcha states (TX/MI/AZ/OK/KS/WI/CO) + schedule DISABLED + ran only 2x → NOT doing the work. SEO reads the `inmates` DB (not live adapters), so EVERY state must be crawled in. ARCHITECTURE SPLIT: direct-fetch DOC sites block datacenter IPs → crawl from RESIDENTIAL IP (local Mac, `run-all-states-crawl.sh` fast tier) or STATE_PROXY_URL; VINE-API + browser/captcha work from CLOUD → the Action. FIXED the Action: broadened default to 19 cloud-capable states (browser/captcha + VINE NY/NJ/KY/CT/NH/TN/WV/MN/RI/VA/NM/MT) + ENABLED daily 07:00 UTC cron + triggered a run. NOW RUNNING: (1) local fast-tier all-states crawl (~45 direct/VINE states, 50 surnames each); (2) Action (7 browser/captcha, cloud); (3) SO grid (40×50=2000 nationwide). COVERAGE JUMPED: inmates 481k rows / 41 states (was ~12); SO 27k / 86 jurisdictions. ADAPTER HEALTH: 41/50 producing; 9 zero = MO/CO/KS (browser-tier, Action handling) + OR/SD/AK/VT/NM/MT (small/NEEDS_FIRST — crawl will reveal if truly broken). Re-check zeros AFTER crawl completes → those still empty = actually-broken adapters to fix. Crawls from LOCAL Mac residential IP.
**2026-07-21 morning (owner sequence 1→3→2):** #1 GAPS: MO (browser, was in neither list) + MT (VINE, dropped from local) closed; Action reordered VINE-first + MO added + re-triggered (run 29843046672, uses fixed code). #3 IndexNow directory re-submitted (2,288 URLs accepted). #2 BROKEN-ADAPTER PROBE → found the real bug: **inmatesDb.upsertInmates had the SAME in-batch-duplicate-id bug as sexOffenderDb (never fixed there)** — ON CONFLICT DO UPDATE can't touch an id twice → whole insert threw → silent 0-write. LA was the tell (80 found/0 upserted → 76 after fix). Hit VINE-heavy states (person under multiple location rows / null inmateId+same name); direct-DOC unique-id states unaffected. FIXED + local crawl RESTARTED with fixed code (recovers silently-failed states) + Action re-triggered. GA:151/MA:5/HI:41 are genuinely THIN (upsert fine), not broken. Coverage this AM: inmates 515k/45 states, SO 63k+ (grid 928/2000). "The rest" (verify new states index, Step 4 de-template, Dependabot 16 vulns/4 high) = backlog. **OWNER PIVOT: pausing inmate data → research SOCIAL PRESENCE data next.**
**RE-SUBMIT IndexNow directory scope after coverage waves finish** (new differentiated states/counties).

**NEXT:** (a) after waves finish: verify new states light up + re-ping IndexNow; (b) verify newly-swept states light up name-in-state/county; (c) Step 3 canonical hygiene, Step 4 de-template; (d) full roster enumeration beyond top-surname seed; (e) county-hub for more states as inmates.county fills in. Noindex refresher given: it's a quality-concentration tool for young domains (avg indexed-page quality drags whole domain), not just a dup-remover; reserve for low-value/residue, not rankable pages.

Death-teaser aside (answered 2026-07-20): BC teaser search returns NO deceased flag (counts+booleans only, no death). Cheap unlock = BC ask to add `deathCount`/`isDeceased` to the teaser identity (they already project criminalCount etc.) — try before licensing ObituaryMonitor. See [[project_signals_augmentation]] + `docs/research/death-data-recon.md`.

**BING + GOOGLE SUBMISSION (2026-07-21):** Bing URL Submission API wired — key `81479d56…` (seo/bing-api-key.txt, gitignored) in seo/.env.local + GitHub repo secret BING_WEBMASTER_KEY. `scripts/gen-bing-priority-urls.mjs` → top ~97 high-value URLs (root+state hubs+county hubs+name-in-state w/ records); `scripts/bing-url-submit.mjs` (quota-aware via GetUrlSubmissionQuota — Bing daily quota is site-specific + SMALL for young domains, was 3/day). Daily cron `.github/workflows/bing-submit.yml` (08:30 UTC). Verified: submitted top 3 → HTTP 200. NOTE: Bing 100/day is a CEILING that grows w/ authority; currently ~low — owner can request increase in Bing WMT. IndexNow ≠ this (IndexNow = discretionary; URL Submission = guaranteed).
**SITEMAP FIX (both engines):** Bing WMT + Google GSC were both submitted `sitemapv2.xml` (conservative 1,000 URLs). SHOULD be `sitemap-directory.xml` (2,291 = quality core incl. 321 county hubs + name-in-state). OWNER: submit sitemap-directory.xml in BOTH Bing WMT + GSC (Sitemaps).
**GOOGLE reality:** NO bulk URL submission (Indexing API = JobPosting/BroadcastEvent only). GSC levers = sitemaps + internal links + manual URL Inspection (request-index ~10-15/day) + Pages report. Pages already emit rich JSON-LD (person/breadcrumb/FAQ/collection). AUTOMATION option: Google service-account added to GSC property → programmatic sitemap submit + URL Inspection API (2k/day — answers the parked "indexed vs discovered" Q at scale) + Search Analytics. Owner must create the service account.
