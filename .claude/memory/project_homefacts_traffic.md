---
name: project_homefacts_traffic
description: homefacts.com partner traffic test (70k visitors) — 4 landing experiences for public-records/background-check/sex-offender intents
metadata: 
  node_type: memory
  type: project
  originSessionId: c559d3ef-73d6-4bd2-ae57-b22ea4c6528f
---

**Test (owner 2026-07-23):** capture ~70k visitors from **homefacts.com** (a PARTNER — confirmed) into our
funnels, instead of their current Intelius/TruthFinder handoff. Design doc: `docs/marketing/2026-07-23-homefacts-traffic-funnels.md`.

**The edge (verified live via Playwright):** homefacts outbound CTAs carry structured params —
`/ads.html?firstName=Robert%20Orlando&lastName=Tapia&city=Victorville&state=California&type=OffenderD_Text`.
As a partner they set these for us, so captured traffic arrives **name+city+state pre-filled** → pay off the
exact promise (primed path), not a cold search. `type` values (`OffenderD_Text`, `offender-details1`,
`Offender_D`/`af_id=31`) = per-placement attribution keys → map to shN+UTM, grade cost-per-trial & trial→paid
BY placement.

**BUILT ✅ 2026-07-23 (bundle public.87c0dbf7.js, NOT deployed):** 3 config-driven routes on VerticalIntentLanding
— /records/sex-offender, /records/background-check, /records/public-records — each AUTO-PRIMES when firstName+lastName
arrive (else cold-search). Teaser engine: FLOW_PRIORITY + SignalTeaser capability-hook flows (background/sexOffender/
publicRecords) + intent-aware copy. **SO FLAG pre-pay** (owner decision): "⚠ Possible offender record for {name} —
unlock to verify" (amber, framed to verify NOT assert); corroborated criminal/SO records stay POST-PAY from licensed
source. Captures homefacts `type` (partner_landing/primed_search events). Verified live. Pre-launch: deploy, partner
URL/type map, getPersonSignals reality check, FCRA-at-payment confirm, state full-name→abbr.

**Original 4 experiences:** (a) Sex-Offender person-primed, (b) Background-Check person-primed,
(c) Background-Check cold-search, (d) Public-Records cold-search. Each = existing landing infra
(OnboardingReveal loader + SupTeaserA + $1→$49 billing); ONLY new work = 3 intent-specific teaser variants
(off `getPersonSignals`) + primed/cold branch + param read + location-framed fallback.

**Gates RESOLVED:** partner ✅, sex-offender compliance sign-off ✅ (still pay off from LICENSED source
Enformion/IDI, NOT re-served registry scrapes; no asserting current-registered status without fresh basis;
keep FCRA + registry disclaimers). Payoff-data reality check understood → location-framed fallback is
first-class (common names → IDI TooManyMatches; NSOPW is location-keyed not person-keyed).

Ties to [[project_life_events_vertical]] (NSOPW/criminal), [[project_signals_augmentation]] (getPersonSignals),
[[project_adunit_funnel_review]] (promise→payoff), Check-Your-Date pilot (same safety report/teaser).

**⚠️ CONVERSION LEAK FOUND + v4 FIX (2026-08-20).** Owner: HomeFacts gets tons of traffic, converts poorly; drop = **abandonment on the FIRST landing page**. Walked LIVE prod (v3 IS deployed): the first thing a cold HomeFacts visitor sees is a **Cloudflare Turnstile "verify you are human" modal over a greyed, spinning page** — because v3 AUTO-FIRES the BC teaser search on mount (the 2026-08-18 "auto-fill on arrival" change), and cold partner traffic (int'l/VPN, was ~60%) is exactly what Turnstile challenges most. Search also 412s until Turnstile solves. Captcha-on-arrival with zero investment = the bounce. (Distinct from captcha on the SEARCH-RESULTS page, which is fine — they've already engaged there.)
- **BUILT: `HomeFactsLandingV4Page.js` (route `/name/landing/homefacts-v4`), commit 9a7e7b0, bundle public.395d91ca.js — NOT deployed.** v4 = v3 with ONE change: search fires on the CTA TAP, not on mount. First paint = the shell (primed identity + "possible offender record — verify" flag + report contents + "See {name}'s full profile →"), **NO search, NO Turnstile**. Tap = the gesture → Turnstile passes; only intent-qualified visitors trigger it. Everything else identical to v3 → clean A/B. Verified real bundle: shell first paint with searchFired=false + turnstileLoaded=false, console clean; tap fires search→resolve.
- **shn 6a7a2af6… UNTOUCHED (still → homefacts-v3).** Owner chose HOLD — eyeball v4 on staging first (open /name/landing/homefacts-v4?firstName=…). **PENDING owner call: "split" (I wire 50/50 A/B v3-vs-v4 in the boot redirect; variant already tracked) or "switch" (point shn → v4).** Root cause note: this reverses the auto-fire decision on FIRST-PAGE evidence.

**⚠️ STRATEGIC PARTNERSHIP (2026-07-29) — supersedes the "traffic test" framing below.** The 70k traffic-switch was DESIGNED + funnels BUILT but is **NOT deployed/live** — turning it on is part of the ask. Now pursuing a deeper deal with **Nations Info Corp (NIC)** — HomeFacts's owner since 2022 (bought from ATTOM/RealtyTrac), David = NIC founder + a major investor in PDS/IDLookup (⚠️ NIC does NOT own IDLookup — never imply it; pitch on strategic value + economics only). Deliverables: `docs/partnerships/homefacts-{strategy.md, deck-slides.md, cover-note.md}` + a published deck artifact. Thesis: HomeFacts is a **people-search engine in a real-estate costume** — #1 organic = sex-offender/person-name; it brokers that traffic to TruthFinder+Intelius (PeopleConnect, **non-exclusive** per owner) for a per-lead fee. We propose: run/**rebuild** its SEO (off the 2013 PHP-5.3 stack) + convert traffic to our OWNED funnel (full LTV vs affiliate sliver) + merge place×person data. Verified facts: **Moz DA 47**, ~2,100 referring domains, domain **est. 1996**, **~135K visits/mo declining** (SimilarWeb), 10.3M indexed pages. TruthFinder/Intelius are OUR competitors, **not NIC's — Zillow is** (real estate). Credibility anchor: David Wolfe built MyLife.com SEO → ~20M/mo Google + 4M Bing (2014–18). Rev-share kept flexible. Owner GST direction: facts straight → sell synergies/combined value → detailed plan → "we could basically rebuild homefacts."

**Funnel break + fix (2026-08-13).** 500+ real HomeFacts visitors/day, but the funnel collapses AT THE CAPTCHA: the co-brand landing AUTO-FIRED the search on arrival (`autoPrime` → `runSearch()`) → dropped visitors into BC Turnstile with NO human gesture → only 12–29% passed (8/12: 531→153; 8/13: 145→17). Everything downstream is residue; ~0 conversions all-time, so NO homefacts billing tag exists (not an attribution bug — no charges happened). Root cause: Turnstile scores on human interaction; a programmatic on-load search has none. (Google People Search doesn't hit this — no name in URL → users type+click → Turnstile passes.) FIX (Move 1+2): `primeToConfirm:true` → land primed traffic on the pre-filled CONFIRM step (initialized at mount, no flash) with the SignalTeaser (value first) + a one-click "See <Name>'s report →" CTA; the click gives Turnstile its gesture. **On a SEPARATE preview route `/name/landing/homefacts-v2` (HOMEFACTS_V2_CFG, variant 'homefacts-v2') — NOT the live experience** (owner: too much traffic on the line; d6afea5). `/name/landing/homefacts` stays LIVE auto-fire. Review side-by-side (`?firstName=&lastName=&state=`), then flip the live cfg only after sign-off. VerticalIntentLanding primeToConfirm handling is gated on the cfg flag so live is untouched. Owner: pushing the CAPTCHA itself back is the deeper fix (later); also a REBILL/attribution check is needed before scaling (shn → BC sale/order + rebill config). Not yet deployed (owner deploys).

**SITE-WIDE partner/channel blackout — found + fixed (2026-08-13).** Reporting agent (bigbot) flagged partner/channel labels went to ZERO for ALL traffic (incl. Google) from Aug 7. Root cause = **commit 97272db (Aug 6)** which removed `partner`/`channel`/`shnName` + the idlookup.me referral override from tracking `data.refer` in `trackingService.buildRefer()`, on the theory BC's `tracking.partner.*` was authoritative — but **bigbot reports off `data.refer`, not BC's `tracking.partner.*`** (confirmed by elimination: GTM was untouched yet Google went blank too → bigbot ≠ GA4). Fix (commit 6cfd2b8): **restore, reconciled via BC-value mirror** — CampaignContext reads BC's own resolved `getShComp('comp.tracking.partner.name'|'channel')` from the shape into `sessionStorage['attribution.bc.*']`; `buildRefer` reports BC's value VERBATIM when present (can't diverge — it IS BC's), falls back to registry identity only where BC has none, **lowercased** (fixes the 'Search'≠'search' miscasing 97272db named). Plus a `partner_attribution_divergence` GTM **drift alarm** (fires if BC≠registry — should stay silent). shn/shl still NOT sent (bigbot: "shN is fine"; most entangled with BC automation). **Validation:** owner's BC source-of-truth sheet matches our registry on EVERY live shN (Internal/Default, Google/Search, NIC/Affiliate); divergent registry entries are placeholder shns absent from BC → nothing to override. getShComp accessor: `comp.`-prefix mandatory, returns `""` (not null/throw) when absent → use `if(!v)`; call `setShapeParams({shn})` before `getShapeCompiled()`. Accessor ref: `.claude/agent-memory/bc-iife-investigator/reference_getshcomp_accessor.md`. NOT deployed (owner deploys). bigbot's **Fault 3** (traffic lands mid-flow on the LOADER, never touches a landing page) = the OLD auto-prime behavior (`autoPrime`→`runSearch()` blew past the landing into /name/loader), **resolved by the primed-confirm default flip below** (b471255) — primed traffic now stops on the confirm landing step. NB attribution CAPTURE runs app-wide (index.js+CampaignProvider), incl. the loader, so shn was captured even on the old path; bigbot's "attribution never runs" was really Fault 1 (shn-only link = nothing to capture, fixed by derivedRefer). **All 3 bigbot faults now addressed in code (F1=derivedRefer ee35580, F2=restore+mirror 6cfd2b8, F3=flip b471255), pending owner deploy.**

**⚠️ REVERSED 2026-08-14 (commit 1e7da32) — auto-fire baseline restored on /homefacts.** Owner: analysis showed ~60% of HomeFacts traffic was bot + international; with that excluded, the ORIGINAL auto-fire ("go to search results") was NOT underperforming. So `HOMEFACTS_CFG.primeToConfirm` is back to **false** — `/name/landing/homefacts` auto-fires the search on arrival again (→ /name/loader). The primed-confirm experience now lives ONLY on **/name/landing/homefacts-v2** (`HOMEFACTS_V2_CFG`, variant 'homefacts-v2'); the two are INDEPENDENT (distinct variant labels isolate metrics), set up as baseline (control) vs challenger before a split test or a full move to v2. NB: `?shn=6a7a2af6…` boot-routes to `/name/landing/homefacts` via registry `landing.route` (NOT -v2; the `normalizeLandingRoute` whitelist rejects `homefacts-v2` and no `awaitTheme`) — verified live + in deployed bundle. Auto-fire re-exposes the Turnstile-no-gesture trade-off (the ~12–29% pass concern) — that's the point of the split test. Verified against local prod bundle: /homefacts→/name/loader, /homefacts-v2→confirm step. NOT deployed (owner deploys).

**HOLD the auto-fire baseline before split-testing (owner 2026-08-14).** The traffic-quality dig confirmed much of the HomeFacts inbound is **international (unusable for us) + bot/datacenter** — so the apparent poor performance was largely junk traffic, not the auto-fire UX. Raw data: `docs/homefacts/2026-08-13 Homefacts Visitors — first on-site URL + params - Sheet1.csv` (972 rows since 2026-07-20; per-visitor IP/hostname/geo/ISP/ASN + bot-UA/proxy/datacenter classifiers + verdict + captcha shown/passed + searched; CONTAINS PII — names in URLs + IPs; bulk verdict='human', a BOT/DC + International excluded slice, ~210 searched). Decision: **give the auto-fire baseline more time to accumulate CLEAN-traffic conversion data before switching to the split experience.** ⚠️ **On 2026-08-15 we may need to implement the A/B split ourselves** (client-side coin-flip in `HomeFactsFunnel` between HOMEFACTS_CFG and HOMEFACTS_V2_CFG) rather than via BC shape — because BC's `theme.landing` can't route to `homefacts-v2` today (the `normalizeLandingRoute` whitelist rejects it; would need the regex widened first). Revisit the split mechanism then. **Analysis cohorts (owner 2026-08-14):** report each arm (homefacts vs homefacts-v2) across **three cohorts — clean visitors, dirty visitors (international + bot/datacenter), and total** — i.e. arm × cohort, not just arm. ⚠️ The clean/dirty verdict comes from OFFLINE enrichment (ip-api.com geo/ISP/ASN + reverse-DNS hostname + bot-UA/proxy/datacenter classifiers), NOT anything the client can compute at request time — so the cohort split has to happen in the reporting/scrub layer that already produces the CSV's `verdict` column, joined to the arm's `variant` tag. Plan: stamp the arm (variant) into the tracking record, then in reporting bucket by verdict∈{clean, dirty} and also show the union (total). The section below is now SUPERSEDED for the default:

**Primed-confirm was the DEFAULT live experience (2026-08-13, commit b471255) — SEE REVERSAL ABOVE.** Owner sign-off after review on /homefacts-v2. `HOMEFACTS_CFG.primeToConfirm=true` — live /name/landing/homefacts now lands primed traffic on the pre-filled confirm step (teaser + one-click CTA) instead of auto-firing into Turnstile (the ~12–29% CAPTCHA break). Variant stays 'homefacts'; -v2 route now mirrors live. Review feedback all incorporated: single top-nav, white co-brand ×, ribbon trimmed to "Continuing your search from HomeFacts", value prop folded into confirm heading ("<Name> — See what public records reveal about them"), mobile sticky bottom CTA. NOT deployed (owner deploys).

**Derived partner attribution (2026-08-13).** Owner saw HomeFacts traffic in **BC tracking** with NO source/medium/campaign/partner-id/shn. Root cause: the partner link is **shn-only** (`?shn=6a7a2af6…&firstName=&lastName=`), and BC tracking `data.refer` (+ order `commerceorders.refer`) reflect ONLY utm_/refer_ params off the inbound URL — none present → all empty. shn drives the shape/session (valid 24-hex → setShapeParams) but isn't stamped on tracking records; BC's shape-level `tracking.partner.*` automation (#77-Q4) isn't populating yet. Fix (owner: "derive our own"): campaign `identity.refer` block mirrors BC DB source-of-truth — **partnerId=nic, source=homefacts, medium=affiliate, campaign=homefacts** (BC `shapecomponents{tracking.partner.name}=nic, .channel=affiliate, .type=aff-network`; shN slug `shn.idlookup.lookup.homefacts`, default shColId `6a7a2a85bdd290c472f90c7b`). CampaignContext persists it to `sessionStorage['attribution.refer']`; trackingService `buildRefer` (tracking) + `buildReferQueryString` (order) **gap-fill** it (real utm/refer_ from URL always wins). ⚠️ **Scoped to utm/refer_ namespace ONLY (source/medium/campaign/partnerId) — NEVER shn/partner/channel field names, which overrode+nullified BC's automation on 2026-08-06.** shn itself still NOT manually sent (same landmine) — must come from BC `tracking.partner`. Commit ee35580, NOT deployed (owner deploys). Generalizes: any campaign can declare `identity.refer`.

**Dedicated HomeFacts landing (2026-08-10, David Teng @ NIC integration):** `/name/landing/homefacts` = `HomeFactsLandingPage` → `VerticalIntentLanding cfg={HOMEFACTS_CFG}` (variant 'homefacts', flow/teaser 'publicRecords', autoPrime, safety+records copy). Link owner gives HomeFacts: `idlookup.ai/name/landing/homefacts?shn=<BC token>&firstName=<>&lastName=<>&state=<>` (owner gets real shn from BC; owner manages consumer builds/deploy). Params: VerticalIntentLanding now reads KEYS CASE-INSENSITIVELY (lc-map `qp()` helper) + aliases (fn/firstname/first, ln/lastname/last, mn/middlename/middle, state/st, city, age) — no casing assumptions. State: `normalizeState` assumes 2-letter, falls back to parsing full names ("California"→CA). **Co-branded SERP header:** `cfg.partnerBrand:'homefacts'` → VerticalIntentLanding persists sessionStorage `idlPartnerBrand` → SearchResultsPage header shows "{brand} × HomeFacts" + "In partnership with HomeFacts" (also falls back to `attribution.shnName/partner` matching /homefacts/i). NOT deployed (owner uploads bundle). The older `/records/*` funnels (homefacts-so/bg/pr) still exist for the 3-intent split; the dedicated route is the single branded experience David requested.

**HomeFacts v3 — PROFILE-FIRST landing (owner 2026-08-18, shipped, staging).** HomeFacts visitors arrive from a single sex-offender profile, so a search form/results list = cognitive dissonance. v3 = `/name/landing/homefacts-v3` (`src/pages/sales/HomeFactsLandingV3Page.js`, isolated new page — NOT VerticalIntentLanding; live /homefacts baseline untouched). Flow: **HomeFacts click → clean loading state ("Finding {name}'s full profile…" + spinner) → AUTO-RESOLVES on mount to the real best-match individual → filled profile → Unlock → `/payment?capture=email&reveal=1` → report for that person.** Key design:
- **First-party shell instant; BC data on resolve.** Pre-resolve is captcha-safe (SignalTeaser/getPersonSignals). The resolve calls `api.searchPeople` (in-page, replicates the loader: response.data→pick best match [city-match else top]→setSearchContext + store `nameSearchResults` + `result_<id>` + `selectedPersonId`). The resolved BC person carries the **extId** so /payment→createReportForIdentity works. Custom v3 layout (NOT SupTeaserA verbatim): PersonAvatar + real aliases + relatives (maskName first+last-initial) + location history + honest masked address/phone/email counts + `foundChips` + "other people named {name}" switcher (selectPerson swaps active).
- **AUTO-FIRE decided KEEP (owner 2026-08-18):** no in-page gesture on mount = the historical captcha exposure, BUT owner: the Turnstile/captcha already renders on the prod search-results path anyway, so auto-fire isn't a new failure + profile-first is better UX than the old blank-loader→captcha. Do NOT revert to a tap. Loading `useState(true)` default; shell only shows as no-match fallback.
- **Account creation DEPRECATED (owner):** unlock skips `/name/signup` → goes to the **email-on-payment** page `/payment?capture=email` (collects email only, auto-generates password, creates account inline in the sale). Added **`reveal=1`** param to PaymentPage: capture=email masks the name for PHONE flows (name = paywalled prize) but reveal=1 (name searches like v3) shows the person PLAINLY (displayPersonName + SignalTeaser anonymize gated on `!revealPerson`). Payment email helper copy changed "We'll set up your account and send your receipt here." → **"So we can email you your report."**
- **Local BC quirks:** dev BC returns 412 (precondition) + shows the **"Input Password" password.v0 captcha modal** — that captcha is OFF on prod per [[project_bc_removed_prod_captcha]], so on staging/prod the auto-resolve should complete without it (the ONE staging check). NEVER suppress the BC captcha modal ([[feedback_never_block_bc_captcha_modal]]). Verified end-to-end on the real prod-bundle (dev BC resolved "Maria Teresa Gonzalez, 32" with 21 relatives/locations/aliases; unlock→payment shows person + email, extId carried). Commits 4f76a22-adjacent: db9b675/56e55f2/f6dc92a/9bea6bb/16419f7/afaab02; deploy bundle public.286de7c6.js. OPEN: staging captcha check; and completing a real purchase to confirm the report is for the right person (the unlock→payment→report seam).

**ROUTED shn traffic → v3 (owner 2026-08-18, commit cd92eaa).** The HomeFacts partner shn `6a7a2af6d8e615c6c6562e8b` registry `landing.route` changed `/name/landing/homefacts` → `/name/landing/homefacts-v3` (campaignRegistry.js), so `idlookup.ai/?shn=6a7a2af6…&firstName=&lastName=&state=&city=&type=` now boot-redirects to the v3 profile-first experience. Works because homefacts has NO awaitTheme → boot redirect uses the PRE-SHAPE registry route (BC theme.landing override happens after). Also widened `normalizeLandingRoute` whitelist to `homefacts(?:-v[0-9]+)?` (allows homefacts-v3) so a shape-enriched re-resolution can't force it back to /homefacts. Verified: /?shn=… → /name/landing/homefacts-v3, shn+params carried, v3 renders. ⚠️ **BC-side follow-up (owner):** BC shape theme.landing for this shN still returns /name/landing/homefacts — update to /name/landing/homefacts-v3 to be airtight. NOT deployed (owner deploys; bundle public.bd702392.js). This makes the v3 staging captcha check (does auto-resolve complete w/o the "Input Password" modal) more urgent — real partner traffic now hits v3's auto-fire.

**shN per URL (owner req, 2026-07-23):** placeholder registry keys in campaignRegistry.js — `homefacts-so` / `homefacts-bg` / `homefacts-pr` (partner=Homefacts, channel=the intent, landing.route null). Verified: ?shn=homefacts-so → attribution.partner=Homefacts + GTM partnerName/partnerChannel. Swap for real BC shConIds when minted. FINAL URLs carry both `type` (fine placement) + `shn` (experience). Teaser fixes 2026-07-23: split mashed firstName, city→teaser subject, teaser renders on ERROR (loader carries subject on error), background/publicRecords lead with real record when found.

**ENTRY-EXPERIENCE PERMUTATIONS v5–v10 BUILT 2026-08-21 (commit 80a22c9, bundle public.8ad69350.js, NOT deployed).** Owner: build ALL permutations, review, then pick rotation. Source-page insight (screenshot docs/homefacts/Image 8-21-26…jpeg): HomeFacts gives address/DOB/physique/aliases free + a literal "VIEW CRIMINAL RECORD" button; we convert ~0 because we show LESS, locked, no map, no criminal-record payoff. Doc: `docs/homefacts/entry-experience-permutations.md` (8 axes, 10 perms, scored). Shared: `src/pages/sales/homefactsShared.js` (subject parse + `useHomeFactsResolve`: TWO-STEP resolveProfile / ONE-STEP resolveAndPay so no-search arms still hand /payment a real person; UI blocks) + `src/components/AreaMap.js` (Nominatim geocode→Leaflet/OSM, safe banner fallback). Arms (all shell-first or no-search — NEVER captcha on arrival): v5 Criminal-Record File (map,criminal-led,2-step) · v6 Instant Map (no-search, resolve on unlock tap) · v7 Dossier (breadth+photo slot) · v8 Proximity/Safety (near-your-family+map) · v9 Mirror HomeFacts (registration-details look + blue VIEW CRIMINAL RECORD btn) · v10 Record Document (redacted sealed record). Verified real bundle: all render + maps load + console clean; routes /name/landing/homefacts-v{5..10}. ⚠️ compliance: v9 mirrors registry look (keep verify framing); v10 redacted values are PLACEHOLDERS (must be real-post-pay or generic before live). Prod: OSM tiles+Nominatim need CSP allowance; move geocode server-side at scale. NEXT (owner order): general `web_events` table (user_id+state, not just funnel) on idlookup.me AFTER these — reuse /api/search-activity Neon+CORS pattern (see project_metrics_harness). Then owner picks arms for rotation.

**A/B TEST WIRED 2026-08-21 (commit 68d2c8a, bundle public.0830ea8e.js, NOT deployed).** Owner: self-assign the arm (not BC's shape). `resolveHomefactsAbRoute` in `src/services/funnelSplit.js` overrides the registry homefacts-v3 route with a sticky `pickWeighted` 33/33/33 across **v5 (Criminal-Record File), v6 (Instant Map), v9 (Mirror HomeFacts)** — placed in HomePageRedirect (App.js) same as the paid split (doesn't await theme). Sticky per SESSION (sessionStorage `split.homefacts_ab`). **BC tracking params (owner's key question): arm reaches BC two ways** — (1) event-level `variant` (funnel.variant via useLandingTrack) on every _sendToBC; (2) attribution/order-level stamped into BC's native A/B slot **`refer_abc`** (buildRefer forwards it onto every event refer + the sale/order). Partner payout unchanged (by shn/refer_partnerId). web_events logs the SAME variant string independently. Verified real bundle: /?shn=6a7a2af6… → homefacts-v5, sticky on reload, funnel.variant + refer_abc = homefacts-v5, even 33/33/33, console clean. GO-LIVE = owner deploys consumer bundle → shn traffic enters the test (no longer v3). Read: web_events GET ?days=N by variant, or Neon SQL. Control note: v3/v4 not in rotation (v3≈0 baseline established).
